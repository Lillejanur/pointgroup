[project]
name = "pointgroup"
version = "0.1.0"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

dependencies = [
    "numpy>=2.4,<3",
    "scipy>=1.17,<2",
    "ase>=3.28,<4",
]